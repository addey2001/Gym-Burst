<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All created workouts</title>
    <%- include('../partials/stylesheets.ejs') %>
    <style>
         body {
            background: linear-gradient(to bottom, #1a237e, #0d133d);
            color: #fff;
            font-family: 'Montserrat', Arial, sans-serif;
            margin: 0;
            min-height: 100vh;
        }
        h1 {
            text-align: center;
            margin-top: 40px;
            font-weight: 700;
            font-size: 2rem;
        }
        ul {
            display: flex;
            flex-wrap: wrap;
            justify-content: center;
            gap: 30px;
            padding: 0;
            margin-top: 40px;
            list-style: none;
        }
        li {
            background: #6ec6ff;
            color: #222;
            border-radius: 20px;
            width: 180px;
            min-height: 80px;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            box-shadow: 0 4px 16px rgba(0,0,0,0.15);
            font-size: 1.1rem;
            font-weight: 600;
            margin-bottom: 20px;
            padding: 20px;
            text-align: center;
        }
          a {
            color: inherit;
            text-decoration: none;
            font-weight: bold;
            font-size: 1.1rem;
        }
        a:hover {
            text-decoration: underline;
        }
        form {
            text-align: center;
            margin-top: 30px;
        }
        button {
            background: #6ec6ff;
            color: #222;
            border: none;
            border-radius: 8px;
            padding: 10px 24px;
            font-size: 1rem;
            font-weight: 600;
            cursor: pointer;
            box-shadow: 0 2px 8px rgba(0,0,0,0.12);
            transition: background 0.2s;
        }
        button:hover {
            background: #2196f3;
            color: #fff;
        }
    </style>
</head>
<body>
    <h1>All created workouts</h1>
    <ul>
        <% workouts.forEach(workout => { %>
            <li>
                <a href="/workouts/<%= workout._id %>"><%= workout.title %></a>
            </li>
        <% }) %>
    </ul>
</body>
</html>